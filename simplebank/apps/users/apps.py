"""App config for the users app."""

from django.apps import AppConfig


class UsersConfig(AppConfig):
    """Django app config for simplebank.apps.users."""

    name = 'simplebank.apps.users'
    label = 'users'
